package io.snowp.tep.arguments;

/**
 * An enum containing all possible arguments so they can be identified in the {@link java.util.HashMap} returned by {@link ArgumentParser#parse(String[])}
 *
 * @author Snowp
 */
public enum Argument
{
    FILE;
}
